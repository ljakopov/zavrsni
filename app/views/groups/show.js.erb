$('#posts').append("<%= escape_javascript(render 'posts/post')%>");
$('#page').html("<%= escape_javascript(link_to_next_page(@posts, 'LOAD MORE', remote: true, id: 'load'))%>");
if (!$('#load').length) { $('#page').remove(); }